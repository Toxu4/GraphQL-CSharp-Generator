{{accessModifier}}{{#unless isConstructor}} {{{resultType}}}{{/unless}} {{name}}({{#each parameters}}{{> Parameter this}}{{#unless @last}}, {{/unless}}{{/each}})
{ 
{{#if body}}
    {{#each body}}
    {{{this}}}
    {{/each}}
{{/if}}
}
